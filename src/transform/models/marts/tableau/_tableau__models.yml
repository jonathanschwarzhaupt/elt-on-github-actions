version: 2

models:
  - name: activity
    columns:
      - name: id
        description: "ID of an activity"
        tests:
          - not_null
      - name: activity_type
        description: "Some type of activity"
      - name: job_title
        description: "The job title"
      - name: request_date
        description: "The month"
        tests:
          - not_null
      - name: mandanten_name
        description: "The name of the Mandant"
        tests:
          - not_null
      - name: project_id
        description: "The ID of the project the Mandant belongs to"
        tests:
          - not_null
      - name: project_name
        description: "The name of the project the Mandant belongs to"
        tests:
          - not_null
